# Program to ask the user a number and prints all the integers numbers until the triple minus one of that number in the console
	.text
QuestNumber:
	li $v0,5
	syscall

li $t0,3
mult $v0,$t0
mflo $a1
subi $a1,$a1,1

li $v0,1
li $t0,0
blt $a1,0,Fim
Ciclo:
	beq $t0,$a1,Fim
	addi $t0,$t0,1
	la $a0,($t0)
	syscall
	j Ciclo

Fim:
	
