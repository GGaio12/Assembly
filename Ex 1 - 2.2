# Program to calculate the sum of the firsts 100 integers numbers and prints it in the console
	.text
li $t0,0
li $t1,100
ciclo:
	addi $t0,$t0,1
	add $v0,$v0,$t0
	beq $t0,$t1,fim
	j ciclo

fim:
	move $a0,$v0
	li $v0,1
	syscall
